{
  "name": "deciflow-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "backend": "cd backend && if not exist .venv python -m venv .venv && .venv\\Scripts\\activate && uvicorn app.main:app --reload",
    "install:all": "npm install --prefix frontend && cd backend && if not exist .venv python -m venv .venv && .venv\\Scripts\\activate && pip install -r requirements.txt"
  }
}
